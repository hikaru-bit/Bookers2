<h1>User info</h1>
<div class="profile-container">
  <h3 class="name"><%= @user.name %></h3>
</div>

<% @users.each do |user| %>
    <p>name : <%= user.name %></p>
    <%= link_to "Show", user_path(users.id)  %>
<% end %>